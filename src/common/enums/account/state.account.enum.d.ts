export declare enum EAccountState {
    ACTIVE = "active",
    INACTIVE = "inactive",
    SUSPENDED = "suspended",
    DEACTIVATED = "deactivated",
    LOCKED = "loked"
}
export declare enum EAccountVerification {
    VERIFIED = "verified",
    UNVERIFIED = "unverified",
    PENDING_APPROVAL = "pending_approval"
}
export declare enum EAccountProcessing {
    NONE = "none",
    PENDING = "pending",
    DELETED = "delete",
    BANNED = "banned"
}
