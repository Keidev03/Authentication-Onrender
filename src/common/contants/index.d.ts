export declare const constants: {
    EXPIRED_CODE: number;
    EXPIRED_ACCESS_TOKEN: number;
    EXPIRED_ID_TOKEN: number;
    EXPIRED_REFRESH_TOKEN: number;
    MIN_EXPIRED_REFRESH_TOKEN: number;
    EXPIRED_SID: number;
};
